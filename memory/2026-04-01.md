- Delivered Supabase schema + RLS (multiempresa) saved at `nexcard-mvp/DB_SCHEMA_SUPABASE.sql`; includes organizations, memberships, profiles, products, orders, order_items, payments, inventory_items, inventory_movements, content_blocks, cards, events; RLS policies for admin/company_owner/company_member/owner; content/public policies for CMS, products, profiles.
- Supabase credentials placed in .env (URL, service role, anon, DB password).
- Could not apply SQL from local due to missing `psql` on macOS 12 (Tier 3, libpq install fails). Instructed user to run SQL directly in Supabase SQL Editor by pasting file contents, not file path. Pending: user to execute the SQL in Supabase and confirm.
- Groq tested with llama-3.3-70b-versatile; works for analysis tasks. Policy: use Groq for light tasks, OpenAI for critical ones. Ollama runs CPU-only; not used further.
- Pending next steps: once SQL applied, cable frontend/backend to Supabase; implement CMS admin, multi-org roles, orders/payments structure.
